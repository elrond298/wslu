#!/bin/bash
printf 'wslsys %s\n' "$*" >> "${WSLU_TEST_LOG:?}"
case "$*" in
  '-B -s'|'--build -s') printf '22631\n' ;;
  --wslfetch\ *) printf 'Build: 22631\nWSL Version: 2\n' ;;
  *) printf 'unexpected wslsys call: %s\n' "$*" >&2; exit 97 ;;
esac
